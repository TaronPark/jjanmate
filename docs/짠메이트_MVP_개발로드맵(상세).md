# 짠메이트 MVP 개발로드맵 (상세) (v1 — 대체됨)

> ⚠️ **본 문서는 2026-08-02부로 [`짠메이트_MVP_개발로드맵_v2.md`](./짠메이트_MVP_개발로드맵_v2.md)로 전면 대체되었습니다.**
> 제품 피벗(레딧형 업보트 커뮤니티)에 따라 AI 태깅·스트릭·매칭 프리뷰·니치 기반 기능들이 폐기되고 새 로드맵으로 재수립되었습니다. 이하 내용은 과거 진행 기록으로만 보존합니다.
>
> ---

> 작성일: 2026-07-19 / 갱신일: 2026-07-26 (런칭 니치 전면 개편 + 자체 닉네임 입력 플로우 + 1~4주차 CRUD·SSR 인증 구조·AI 태깅·스트릭·리액션·재시도 큐·이미지 첨부 구현 + 5주차 시드 콘텐츠 드립 크론·매칭 프리뷰 실데이터 전환·시드 반응 자동 생성·프롬프트 튜닝(니치 정확도 95.9%) + Vercel 플랜 게이트 정정(크론 개수 제한 해소 반영) + 6주차 법률 검토 게이트 완료(이용약관 개정, 개인정보처리방침 수정 불필요 확정) + 5주차 CBT 직전 UI/UX 폴리싱 스프린트 편입)
> 본 문서는 「짠메이트 MVP 기획서」의 실행을 위한 시간순 작업 목록입니다. 전략·기능 정의는 기획서를 따르며, 본 문서는 그 실행 산출물과 순서만 다룹니다. 각 작업은 기획서의 관련 섹션과 연결됩니다.

## 0단계: 개발 착수 전 준비

| 작업 | 관련 기획서 섹션 | 비고 |
|---|---|---|
| 서비스명·도메인 확정 및 선점 | 표지 | **완료 (2026-07-22)**. 서비스명 "짠메이트" 확정, jjanmate.com·jjanmate.kr 도메인 5년 등록 완료(후이즈, WHOIS 정보보호 서비스 적용). 국문 워드마크 상표 출원 진행 중(09·38·42류, 특허청 제출 대기 — "출원준비" 단계, 우선심사는 신청하지 않음). 영문 상표·로고 상표는 보류 |
| DB 스키마/ERD 설계 (테이블·컬럼·관계 정의) | 5, 6, 9 | 1~2주차 첫 작업과 직결 |
| 화면 와이어프레임 (온보딩 3~4장, 피드, 게시 화면) | 4-A, 4-B, 5 | 개발자(본인) 착수 기준선. 2026-07-21 사용자 검토로 v2 확정 (블러 제거, 카카오 단일 로그인, AI 처리중 전환화면, 니치룸+리액션 반영) |
| 인프라 계정 셋업 (Vercel, Supabase, Claude API 키, 카카오 개발자 앱 등록, 리포지토리) | 7 | 개발 착수의 실질적 0단계. 카카오 단일 로그인 확정으로 카카오 개발자 앱 등록 추가. **리포지토리는 완료 (2026-07-23)**: `github.com/TaronPark/jjanmate`에 Next.js 16.2.11 + React 19 스캐폴딩(온보딩/프리뷰/로그인/게시/피드 6화면 골격) 최초 커밋·푸시 완료, `docs/`에 기획서·로드맵·ERD·schema.sql 사본 포함. 나머지(Vercel, Claude API 키, 카카오 개발자 앱 등록)는 아직 미완료 |
| 개인정보처리방침/이용약관 초안 | 5 (소셜 로그인) | **초안 작성 완료 (2026-07-25)**. `/terms`, `/privacy` 페이지로 실제 배포·연결 완료(로그인 화면에서 링크). 카카오 로그인이 실제 라이브된 시점이라 우선 반영함. AI(Claude API) 처리 위탁·국외이전(Supabase 싱가포르/Vercel·Anthropic 미국) 고지 포함. **단, 법률 자문이 아닌 초안이므로 정식 공개 전 개인정보보호 전문 법률가 검토 필수(6주차 소프트 런칭 게이트로 명문화, 아래 참고)** — 사업자등록은 여전히 후순위 가능 |
| 거지맵 최신 기능 재확인 | 1, 11 | **완료 (2026-07-23)**. 신규 기능 "경유쇼핑"(제휴 쇼핑몰 캐시백) 확인, AI 매칭·니치 커뮤니티 기능은 미확인(차별화 축 안전). "야장맵" 등 초니치 지도앱 유행 확인 — 기획서 1·11번 반영 완료 |

## 1~2주차

| 작업 | 관련 기획서 섹션 |
|---|---|
| **완료 (2026-07-25)**: DB 테이블 실제 생성 — `reactions` 테이블 신규 생성, profiles/posts/matching_previews 3개 테이블 니치 CHECK 제약조건을 2026-07-24 개편값(monthly_rent_fighter 등)으로 ALTER 마이그레이션, 4개 테이블 RLS 활성화 및 정책 적용까지 모두 완료(ERD 5-1, schema.sql 참고) | 5, 7 |
| **완료 (2026-07-21 초기 연동 / 2026-07-25 SSR 구조로 전면 개편)**: 카카오 단일 소셜 로그인 구현 — 세션을 쿠키 기반(`@supabase/ssr`)으로 전환, `middleware.ts` 세션 갱신, `app/auth/callback/route.ts`에서 인가코드 교환 + 신규/재방문 유저 분기(프로필 유무로 `/nickname` 또는 본인 홈룸 피드) | 5, 7 |
| **완료 (2026-07-25)**: 짠메이트 전용 닉네임 직접 입력 화면 실연동 — `app/nickname/actions.ts` Server Action으로 `profiles.nickname` upsert, 재방문 유저는 콜백 라우트 단계에서 이미 걸러져 이 화면 자체에 도달하지 않음(스킵 분기 완료). 카카오 닉네임/프로필사진 미사용 결정에 따름 | 4-A, 5 |
| **완료 (2026-07-25)**: 게시 CRUD — `app/post/actions.ts` Server Action으로 `posts` insert, 300자 제한은 서버·DB 양쪽에서 검증. status/ai_niche 등 AI 판단 필드는 클라이언트가 절대 넘기지 않고 DB 기본값(pending/null) 유지 | 5, 6, 11 |
| **완료 (2026-07-25)**: 니치별 피드 실데이터 조회 — `app/feed/[niche]`를 Server Component로 전환해 `posts`+`profiles` join 조회. RLS가 `status='success' AND is_spam=false` 또는 본인 글만 자동으로 걸러주므로 쿼리는 니치 필터만 명시. 비회원(anon 세션)도 동일 쿼리로 동작해 "비회원 피드 열람" 요건도 함께 충족. **단, 3주차 AI 태깅 전까지는 `ai_niche`가 항상 null이라 실제로는 피드가 비어보임(정상 상태, 버그 아님)** | 5 |
| 온보딩 니치 선택 화면 구현 (UI 완료, DB 연동은 닉네임 저장 시점에 함께 처리됨) | 4-A, 5 |
| **완료 (2026-07-25)**: 게시글 이미지 첨부(Supabase Storage 연동) — `post_images` 버킷(공개, `file_size_limit=5MB`, `allowed_mime_types=image/*`) + RLS(`storage.foldername(name)` 기반 폴더 스코프 INSERT, `owner` 기반 UPDATE/DELETE, 불필요한 SELECT 정책은 `get_advisors` 지적으로 제거해 버킷 오브젝트 나열 방지). 클라이언트에서 `browser-image-compression`으로 선택 즉시 리사이즈(긴 변 1600px)·압축(품질 0.8)·EXIF 회전 보정 후 업로드, `app/post/actions.ts`의 `createPost`가 저장 전 `image_url`이 실제 이 버킷의 공개 URL 패턴으로 시작하는지 서버에서 검증(임의 외부 URL 저장 차단). 피드 카드는 `next/image` 대신 기본 `<img>` 태그로 렌더링(Vercel 이미지 최적화 한도 초과 방지 목적), `object-fit: cover`·`max-height`·`border-radius`로 레이아웃 방어 | 5 |

## 3주차

| 작업 | 관련 기획서 섹션 |
|---|---|
| **완료 (2026-07-25)**: 니치별 판단 가이드라인 확정 — `docs/짠메이트_AI_태깅_프롬프트_가이드라인.md`(니치별 판단 신호, 식비 하드룰, 온보딩 힌트 타이브레이커, 스팸/저신뢰 기준, 서브태그 고정 목록 정책, 과적합 방지 문구) | 6, 11 |
| **완료 (2026-07-25)**: Claude API 연동 및 프롬프트 설계 (niche, subtags, is_spam, spam_reason, confidence 스키마) — `lib/ai/classifyPost.ts`, tool use 강제 호출로 JSON 형태 보장. status는 별도 서버 로직(confidence<0.6 → low_confidence, 실패 → system_error)이 계산 | 6 |
| **완료 (2026-07-25)**: 프롬프트 인젝션 방어 설계 반영 (`<user_post>` 구분자 격리, 유저 입력=데이터 명시, 지시문 무시 규칙 포함) | 11 |
| **완료 (2026-07-25)**: status 분기 처리 및 격리 로그(spam_reason 포함) 저장 — `app/api/tags/route.ts`(postId만 받아 서버가 직접 재조회, RLS로 본인 글만 허용) + `lib/supabase/admin.ts`(service_role, posts UPDATE 전용 통로) + `app/post/actions.ts` 연동 | 6, 9 |
| **완료 (2026-07-25, 게시 흐름 연동)**: 게시 직후 `createPost` 안에서 태깅을 동기 실행해 "AI가 읽는 중..." 전환 화면과 실제 처리 시간이 맞물리도록 함(4-C Labor Illusion 유지). **남은 작업**: system_error 배너 UI(피드/게시 화면에 실패 상태 노출)는 아직 미구현 | 4-A, 4-C |
| **실제 검증 완료 (2026-07-25)**: 라이브 Vercel 배포본에서 실제 게시글로 E2E 테스트 — "라면 소분해서 부대찌개 2인분으로 늘려 먹기"(냉장고 활용) 글이 식비 하드룰대로 `ai_niche='monthly_rent_fighter'`, `subtags=['냉장고파먹기']`, `confidence=0.850`, `status='success'`, `niche_hint_mismatch=false`로 정확히 분류되어 해당 니치 피드에 즉시 노출됨을 DB 조회로 확인 | 6 |
| **남은 작업**: `retry_count` 기반 실제 재시도 실행(백그라운드 큐)은 4주차 항목으로 유지 — 이번 작업은 system_error 시 상태·카운트만 정확히 기록 | 6, 9 |
| 매칭 프리뷰 배치(Cron) 스크립트 구현 (닉네임 마스킹, 니치별 CTA 문구 분기 포함) | 5, 9 |

## 4주차

| 작업 | 관련 기획서 섹션 |
|---|---|
| **완료 (2026-07-25)**: 스트릭 카운트 로직 — `lib/streak.ts`의 `updateStreakOnPost()`(service_role). KST(UTC+9) 기준으로 오늘/어제/그외 3분기 처리, `longest_streak = max(...)` 공식 통일, 실패해도 게시 흐름을 막지 않도록 내부 try/catch. 이 작업의 전제로 `profiles_update_own` RLS 정책을 완전히 제거(클라이언트의 통계 필드 직접 조작 차단, 마이그레이션명: `drop_profiles_update_own_for_streak_integrity`) | 5 |
| **완료 (2026-07-25)**: 피드 상단 고정 상태 카드 구현 — `app/feed/[niche]/page.tsx`에서 비회원(로그인 유도 CTA)/로그인+오늘 게시함(스트릭 축하)/로그인+오늘 미게시(글쓰기 CTA) 3분기, 헤더 🔥 배지는 "유효 스트릭"을 화면에서 파생 계산(DB 값을 그대로 안 믿고 `last_post_date`가 오늘/어제 KST가 아니면 0으로 보정 — 별도 자정 초기화 배치 없이 항상 정확) | 4-B |
| 주간 랭킹 + 랭킹 노출 여부 토글 UI (**보류, Should로 후순위 조정 — 2026-07-25**: DB 스키마(`weekly_rankings`, `ranking_visible` 토글)가 ERD 설계 당시 의도적으로 제외됨. 5주차 소프트 런칭을 앞두고 신규 스키마 설계까지 벌이는 건 일정 리스크가 커서 백로그로 이동) | 5, 11 |
| **완료 (2026-07-25)**: 백그라운드 재시도 큐 구축 — `app/api/cron/retry/route.ts`(Vercel Cron, `CRON_SECRET` 인증, `maxDuration=60`), `status='system_error' AND retry_count<3` 조건으로 최대 20건 배치 조회 후 `lib/ai/classifyPost.ts`의 `runTaggingPipeline`을 그대로 재호출(중복 구현 없이 성공/저신뢰/재실패 3분기 재사용 — 성공·저신뢰로 전환되면 status가 바뀌어 자동으로 큐에서 제외됨, 3회 소진 시 system_error로 남아 수동 검토 대상). `vercel.json` 신규(Hobby 플랜 제약상 하루 1회, `0 19 * * *` UTC = KST 새벽 4시) | 6, 9 |
| **완료 (2026-07-25)**: 원클릭 공감 리액션 기능 — `app/feed/actions.ts`의 `toggleReaction()`(select-then-insert/delete, DB unique 제약으로 race 방어), 피드 쿼리에 `reactions(reaction_type, user_id)` nested select로 카운트/본인 반응여부 계산, `app/feed/[niche]/ReactionButtons.tsx`(클라이언트 컴포넌트, useTransition 낙관적 업데이트, 비회원 클릭 시 confirm 후 `/login` 이동, 활성 상태 스타일+카운트 표기) | 5, 6 (2026-07-21 Must 승격, 신규 추가) |
| **완료 (2026-07-25)**: 비회원 피드 진입점 + 로그아웃 버튼 — `app/login/page.tsx` 하단에 "로그인 없이 먼저 구경하기" 링크(→ `/feed/lurker_lounge`)로 기획서 5번 Must "비회원 피드 열람" 충족. `components/LogoutButton.tsx`(신규 최초의 공용 컴포넌트 디렉토리) 신설, 피드 헤더 우측(로그인 시에만)에 배치 — 전역 레이아웃은 건드리지 않아 랜딩/프리뷰/약관 등 하드코딩 유지 페이지에 영향 없음 | 5 |
| **완료 (2026-07-25)**: 스팸 격리 건 확인용 경량 DB 조회 스크립트 — `docs/짠메이트_운영_스팟체크_쿼리.md`(① 스팸 격리 최근 50건, ② 재시도 3회 소진 최종 실패건, ③ 오늘 하루 KST 기준 상태별 통계). Gemini 제안 원안 검토 중 ③번의 KST 자정 계산 버그(`AT TIME ZONE` 이중 체이닝, 자정 truncate 누락 — 시간대에 따라 날짜가 하루씩 밀리는 간헐적 오류) 발견해 `date_trunc` 방식으로 수정, 라이브 DB로 실측 검증함. 별도 관리 백엔드 없이 운영진이 Supabase SQL Editor에서 직접 실행 | 9, 10 |

## 5주차

| 작업 | 관련 기획서 섹션 |
|---|---|
| **완료 (2026-07-25)**: 시드 콘텐츠 드립(Drip-feed) 발행 크론 + 매칭 프리뷰 배치 — `seed_contents_pool` 테이블(RLS 활성화, 정책 없음 — service_role 전용) + 동시성 방어용 `claim_seed_contents(niche, limit)` RPC(`FOR UPDATE SKIP LOCKED` 원자적 추출, 라이브 DB로 중복 미발행 실측 검증). `app/api/cron/drip/route.ts`가 하루 4개 크론(KST 08/12/18/23시, 15/20/25/40% 비중, 최대잉여법으로 반올림 오차 없이 일일 목표치 정확히 합산)으로 니치 3개에 균등+로테이션 배분해 발행. **테이퍼링**: `LAUNCH_DATE` 기준 1~7일차 30개/일 → 8~14일차 10개/일 → 15일차~ 2개/일로 자동 축소(코드 재배포 없이 날짜 계산만으로 전환). 발행 직후 매번 3개 니치 전체의 `matching_previews`를 갱신(오가닉 게시글도 항상 반영, 유저 식별 정보는 저장하지 않고 content/image_url만 저장). 니치별 시드 계정 생성 및 실제 콘텐츠(문구+사진) 확보는 운영 작업으로 별도 진행 | 5, 9 |
| **완료 (2026-07-25)**: 매칭 프리뷰 화면(`app/preview/page.tsx`) 실데이터 전환 — 기존 client + Suspense 우회 방식을 async Server Component로 리팩터링(`searchParams`를 prop으로 직접 받아 `matching_previews`를 서버에서 조회), 로그인 CTA 버튼만 `components/LoginButton.tsx`로 분리. 프리뷰 데이터가 아직 없는 니치는 대체 문구로 방어 | 5 |
| **완료 (2026-07-26)**: 시드 계정 30개 + 콘텐츠 400개(니치별 130/240/30) DB 인서트, 드립 크론 실행 검증(수동 트리거로 claim→posts insert→매칭 프리뷰 갱신 전체 파이프라인 확인) | 5, 9 |
| **완료 (2026-07-26)**: 드립 크론에 시드 반응(reactions) 자동 생성 로직 추가 — 신규 발행 게시글마다 다른 시드 페르소나 3~7명이 cheer/me_too 반응을 남긴 것처럼 실제 row로 bulk insert. 오가닉 유저 반응과 완전히 동일한 테이블 구조/카운트 로직 사용 | 5, 6 |
| **완료 (2026-07-26)**: 프롬프트 튜닝(시드 콘텐츠로 태깅 정확도 검증) — 식비 엣지케이스 60건으로 4라운드 검증, 니치 일치율 83%→95.9%까지 개선(식비 하드룰 우선순위화 + 장기 습관 교정 조항 + 추론 순서(CoT) 도입, `docs/짠메이트_AI_태깅_프롬프트_가이드라인.md` 참고). 잔여 `low_confidence` 이슈는 `docs/짠메이트_운영_이슈트래커.md` 1-3에 백로그로 이관 | 6, 9 |
| **[신규, 2026-07-26]** UI/UX 폴리싱 스프린트 — 현재 UI로 CBT를 시작하는 게 무의미하다고 판단해 CBT 직전에 편입. 하단 탭 바(홈/둘러보기/마이페이지) + FAB 글쓰기 + 피드 카드 상대시간·타이포그래피 + 리액션 아이콘 칩 개편. 백엔드/DB/AI 파이프라인은 무변경. 상세 스펙은 `docs/짠메이트_프론트엔드_UIUX_개편_스펙.md` 참고 | 4-A, 4-B, 4-C, 5 |
| 비공개 베타(CBT) 진행 | 9 |
| KPI 초기 측정 시작 (니치당 활성 게시자 수, 태깅 정확도, 3일 스트릭 비율, 게시글당 평균 공감 반응 수) | 10 |

## 6주차

> **소프트 런칭 게이트 (2026-07-25 추가):** 아래 항목들을 통과하기 전에는 자연 확산 채널 소프트 런칭에 착수하지 않는다. 개인정보처리방침·이용약관은 현재 초안(2026-07-25 시행, 박대성 개인정보 보호책임자 기재) 상태로 베타 배포본에는 이미 연결돼 있으나, 정식 공개 전 반드시 재검토가 필요함(0단계 로드맵 참고).

| 작업 | 관련 기획서 섹션 |
|---|---|
| **[게이트] 개인정보처리방침·이용약관 법률 전문가 검토 완료 및 재개정 — 완료 (2026-07-26)** — 국외이전 고지(Supabase 싱가포르/Vercel·Anthropic 미국), 만 14세 미만 제한, Anthropic 학습 미사용 조항의 최신 약관 대조 포함. 5주차 시드 콘텐츠 드립 발행(운영진이 사전 작성한 글을 시드 계정 명의로 지속 발행)의 고지 필요 여부도 검토 범위에 포함함. **검토 결과**: 이용약관 제8조 3항(시드 콘텐츠 등록·노출 근거) 신설 + 제15조 면책사유 추가 및 3항 신설로 반영 완료(`app/terms/page.tsx`, `docs/짠메이트_이용약관.md`). 개인정보처리방침은 시드 콘텐츠가 개인 식별 데이터와 무관해 수정 불필요로 확정(`docs/짠메이트_개인정보처리방침.md`) | 5, 11 |
| **[게이트] `npm audit` 재확인 (2026-07-25 보류 결정)** — `@supabase/ssr` 도입 시점에 Next.js의 하위 의존성(postcss, sharp)에서 high severity 취약점 3건 발견. `npm audit fix --force`는 Next.js를 9.3.3(App Router 이전 버전)으로 강제 다운그레이드시켜 앱 전체가 깨지므로 사용 금지. 현재는 두 패키지 모두 우리 앱이 실제로 쓰지 않는 경로(빌드타임 CSS 처리, 미구현 이미지 업로드)라 실질 위험 낮다고 판단해 보류. Next.js 패치 버전이 하위 의존성을 올려줄 때까지 대기하거나, 필요 시 `package.json`의 `overrides`로 postcss/sharp만 개별 교체하는 방안을 소프트 런칭 전 재검토 | 7 |
| **[게이트] Vercel 플랜(Hobby→Pro) 업그레이드 필요성 재검토 (2026-07-25 작성 / 2026-07-25 정정)** — 4주차 백그라운드 재시도 큐(`app/api/cron/retry`) + 5주차 시드 콘텐츠 드립 크론(`app/api/cron/drip`, 하루 4개 스케줄)까지 총 5개 크론을 Hobby 플랜으로 운영 중. **정정 사항**: 최초 작성 시점엔 "프로젝트당 Cron 최대 2개" 제약이 있다고 기록했으나, Vercel이 2026-01-20부로 크론 개수 제한을 전 플랜 100개로 완화해 더 이상 유효하지 않음(5주차 작업 중 재확인, [출처](https://vercel.com/changelog/cron-jobs-now-support-100-per-project-on-every-plan)) — 개수 제약은 해소됨. 여전히 남아있는 Hobby 제약은: 크론 1개당 하루 최대 1회 실행(그보다 잦은 스케줄은 배포 자체가 실패, 그래서 드립 크론도 "하루 4번 도는 크론 1개"가 아니라 "각각 하루 1회씩 도는 크론 4개"로 구현함), 함수 실행시간 최대 60초(Fluid Compute 미적용 기준), 스케줄된 "시" 안에서 정확한 실행 분은 Vercel이 임의로 정함(분 단위 정밀도 없음 — 드립 크론은 실행 시점의 실제 KST 시각을 직접 읽어 비중을 계산하므로 이 오차에 영향받지 않도록 설계함). 이 때문에 `system_error` 글의 "최대 3회 재시도"가 최악의 경우 3일에 걸쳐 완료될 수 있음 — 지금은 게시량이 적어(스팟체크로 충분) 허용 가능한 리스크로 보고 그대로 감. **분 단위 정밀 스케줄링이 필요해지면 그때 Pro 업그레이드를 재검토할 것(크론 개수는 더 이상 업그레이드 사유가 아님)** | 6, 7, 9 |
| 자연 확산 채널(에브리타임·인스타·커뮤니티 카페) 소프트 런칭 | 9 |
| KPI 본격 추적 (7일 리텐션, 니치 재분류 비율, 스팸 오탐/미탐률, 스팟체크 시간) | 10 |

## 런칭 이후

| 작업 | 관련 기획서 섹션 |
|---|---|
| KPI 데이터 기반 수익모델 우선순위 재점검 | 8 |
| 카카오 로그인 전환율 데이터 기반 구글 로그인 추가 여부 재검토 | 5, 11 |
| 짠포인트/제휴 커머스 도입 전 법률 검토 착수 | 8, 11 |
| AI 태깅 결과(`niche_hint_mismatch`) 기반 룸 이동 추천 배너 (2026-07-24 추가, 니치 개편 후에도 유효) — 눈팅러 대기실 유저가 실제로는 월세독립군/홧김비용방어 패턴으로 태깅될 경우 해당 룸 이동을 유도. 별도 스키마 변경 없이 기존 `niche_hint_mismatch` 필드로 구현 가능 | 3, 4-B, 6 |
| 백로그 니치 4종(K-캥거루 독립 준비, 숨고르기 0원 생존기, 잔바리 지출 철벽, 신혼부부·2인가구) 도입 검토 — 잔바리는 홧김비용방어 룸 트래픽·`niche_hint_mismatch` 데이터 기반으로 분리 시점 판단 (2026-07-24 추가) | 3 |
| 피드 화면 룸 타이틀 시각적 강조 UI 개선 (폰트/아이콘 등, 2026-07-24 전략검토에서 제기) | 4-C |
