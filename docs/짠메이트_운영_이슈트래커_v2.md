# 짠메이트 운영 이슈 트래커 v2

> 최초 작성일: 2026-08-02
> 레딧형 업보트 커뮤니티(v2 아키텍처) 기준 운영 이슈 트래커. 구버전(AI 태깅/니치 매칭 시절)은 [`짠메이트_운영_이슈트래커.md`](./짠메이트_운영_이슈트래커.md)에 과거 기록으로 보존되어 있고, 이 문서와는 무관합니다.
> 각 항목은 다음 형식을 따릅니다: **현황 → 결정/상태 → 재검토 트리거**.
> 2026-08-08부터 개발은 Cowork 대신 VS Code + Claude Code로 진행. Cowork와 Claude Code 세션 간 컨텍스트가 자동 공유되지 않으므로, 진행 상황·발견 사항은 이 문서에 그때그때 기록.

---

## 1. 시드 데이터 재구성 — 인서트/셋업 완료, 런칭일 대기 중

- **현황**: 사용자가 직접 작성한 400개 세이드 콘텐츠(`짠메이트_시드콘텐츠_400개_작성템플릿.xlsx` 기반)를 `seed_contents_pool`에 전량 인서트 완료(2026-08-02). 룸별 분포는 자취룸68/C발비용68/식비절약68/직장생활68/짠수다64/티끌모으기64 = 총 400개로 확인됨. 시드 페르소나도 30명 → 80명으로 확장 완료.
- **결정 (2026-08-02)**: `app/api/cron/seed-drip/route.ts`를 v1과 동일한 테이퍼링(1~7일차 30개/일 → 8~14일차 10개/일 → 15일차~ 2개/일, 룸별 잔여 풀 비례 배분, 8/12/18/23시 KST 시간대별 가중 발행)로 재설계하고, `vercel.json`에 4개 시간대 크론을 등록함. v1에 없던 기능도 추가: 새 글마다 다른 시드 페르소나의 업/다운보트(4~14명, 80% 업보트)와 짧은 시드 댓글(2~5개, 일부 대댓글·베스트댓글 고정(+10)·다운보트 접힘(-5) 임계치를 넘기도록 투표까지 자동 생성) — 핫스코어 정렬·베스트 댓글 고정·접힘 UI가 실제로 작동하는 것처럼 보이게 하기 위함.
  아직 QA/회귀 테스트가 안 끝난 단계라 실제 발행 시작 시점(런칭일)은 지금 정하지 않기로 함 — 환경변수 `SEED_DRIP_LAUNCH_DATE`가 Vercel에 설정되기 전까지 크론은 아무 것도 안 하고 조용히 종료하도록 만들어둠(크론 스케줄 자체는 등록돼 있어도 안전). `SEED_DRIP_LAUNCH_DATE`는 일단 2026-09-05로 잠정 설정(로컬 `.env.local`에만 반영, Vercel 대시보드에는 아직 미반영 — 사용자가 직접 등록 필요), 향후 QA 완료 시점에 맞춰 재조정 예정.
  런칭일 이전 클램프 버그(미래 런칭일을 설정해도 daysSinceLaunch 클램프 때문에 당일 즉시 Phase1이 발행되던 문제)를 `getTodayKst()` 명시적 문자열 비교로 수정 완료(commit `ce025b4`).
- **DB 레벨 드라이런 검증 (2026-08-02)**: 실제 크론을 실행하지 않고 읽기 전용 쿼리로 데이터 정합성만 확인 — 시드 페르소나 수 80명 일치, 룸별 미발행 잔여 풀이 인서트한 분포와 정확히 일치, `posts`/`comments`/`votes` 테이블이 모두 0건(조기 발행 없음 확인), 4개 결정형 플레어(`buy_or_not`/`should_i_buy`/`job_change`/`room_suggestion`)에만 `one_line_question`이 채워져 있음을 확인.
- **라이브 테스트 (2026-08-02)**: `git push` + Vercel `SEED_DRIP_LAUNCH_DATE=2026-08-02` 임시 설정 후 재배포하여 실제 배포 엔드포인트(`jjanmate.vercel.app/api/cron/seed-drip`)를 직접 호출. Phase 1(1일차, 30개/일, 23시 슬롯 40% 가중치=12개) 로직대로 발행되는 것 자체는 확인했으나, 테스트 도중 짧은 시간 안에 요청이 겹치면서(연속 호출) **중복 발행 버그**를 발견함 — `seed_contents_pool`에서 미발행 항목을 SELECT로 조회한 뒤 별도 UPDATE로 `posted_at`을 표시하는 2단계 방식이라 그 사이 다른 요청이 같은 항목을 집어갈 수 있었음(실제로 게시글 49건 중 1건이 동일 항목 중복 게시로 확인됨). 정상 운영에서는 Vercel 크론이 정시에 1회만 실행되므로 거의 발생하지 않지만, 방어적으로 `claim_seed_pool_items()` Postgres 함수(`UPDATE ... FOR UPDATE SKIP LOCKED`)로 조회+표시를 원자적 단일 쿼리로 교체해 수정함(commit `33dde21`). 테스트로 생성된 posts/comments/votes는 전량 삭제하고 `seed_contents_pool.posted_at`도 전부 NULL로 복원해 400개 풀이 온전한 상태로 되돌아감.
- **남은 작업 처리 완료 (2026-08-08)**: ① 버그 수정 커밋(`33dde21`)은 이미 `origin/main`에 반영되어 있음을 확인. ② Vercel 환경변수 `SEED_DRIP_LAUNCH_DATE`를 대시보드에서 확인한 결과 실수로 비어있던 것을 발견 — `2026-09-05`로 새로 설정 후 재배포 완료(값 자체가 "Sensitive"로 저장돼 이전 값이 실제 무엇이었는지는 확인 불가능했음, 그냥 확정값으로 덮어씀). ③ 서울 이전 후 전체 플로우 회귀 테스트 완료(Claude Code + Claude in Chrome `@browser`로 로그인/글쓰기/피드/투표/댓글/북마크/마이페이지/알림/설정 확인, 콘솔 에러 0건·네트워크 요청 전부 정상).
- **CBT 시작일 최종 확정 (2026-08-08)**: `SEED_DRIP_LAUNCH_DATE=2026-09-05` 유지로 확정. 이 날짜부터 시드 드립이 실제로 발행 시작됨.
- **재검토 트리거**: 없음 — 발행 시작일 확정 완료. 4번 항목(서울 이전 환경변수 재점검)도 함께 참고할 것.
- **관련 코드**: `app/api/cron/seed-drip/route.ts`, `vercel.json`, `.env.local`/`.env.local.example`(SEED_DRIP_LAUNCH_DATE), `claim_seed_pool_items()` DB 함수

## 2. 룸/인기 피드 성능 — 구조적 지연 여지 남음

- **현황**: 룸 드롭박스로 룸을 바꿀 때 체감 지연이 크다는 리포트가 있어 확인해보니, (a) 서버 컴포넌트가 룸 조회·룸 목록·로그인 확인을 순차 await하고 있었고 (b) `loading.tsx`가 전혀 없어 응답을 기다리는 동안 화면이 멈춘 것처럼 보였음. 두 가지 모두 수정 완료(`app/room/[code]/page.tsx`·`app/page.tsx` 병렬 fetch로 변경, `app/loading.tsx`·`app/room/[code]/loading.tsx` 스켈레톤 추가).
- **결정 (2026-08-02)**: 애플리케이션 레벨에서 고칠 수 있는 부분은 처리 완료. 다만 Supabase 프로젝트 리전(ap-southeast-1)과 Vercel Hobby 플랜의 서버리스 콜드스타트는 구조적 요인이라 이번 작업으로 완전히 없어지지는 않음 — 사용자가 직접 배포 환경에서 체감 확인함(정상 작동 확인됨, 2026-08-02).
- **재검토 트리거**: 실사용자 유입 후에도 룸 전환 체감 지연이 반복적으로 불편하다는 신호가 있으면, ① Supabase 리전을 한국에 더 가까운 곳(현재 서울 리전 미지원 시 도쿄 등)으로 이전하거나 ② Vercel Pro로 업그레이드해 Edge/리전 옵션을 확보하는 방향을 검토.
- **후속 조치 완료 (2026-08-07)**: Supabase 프로젝트를 싱가포르 리전에서 서울(icn1) 리전으로 이전 완료(신규 프로젝트 ref `egutchfhvysqclxgyixj`, 구 프로젝트 `tmawuxjllizzixancfyg`는 Kakao Provider 비활성화 후 백업용으로 보존). Vercel Function 리전도 icn1로 맞춰서, 기존에 있던 "Vercel(미국 동부)↔DB(싱가포르)" 대륙간 왕복 지연이 사라짐. 로그인 확인 완료, 정상 운영 중. 위 ①번 방향으로 구조적 요인 자체가 해소된 상태 — 다시 지연 리포트가 들어오면 그때는 리전 문제가 아니라 다른 원인(쿼리/캐싱 등)일 가능성이 높음.
- **관련 코드**: `app/page.tsx`, `app/room/[code]/page.tsx`, `app/loading.tsx`, `app/room/[code]/loading.tsx`, `lib/feed.ts`

## 3. QA 중 수정요청사항 반영 (2026-08-03 ~ 2026-08-05) — 처리 완료

- **현황**: 10단계(QA) 진행 중 사용자가 실제 화면을 훑어보며 정리한 수정요청 목록. 2026-08-03에 8건, 2026-08-05에 두 차례(1/3/4번 3건 + 5-1/5-2/5-3번 3건) 총 6건 접수됨.
- **결정 (2026-08-03, commit `fdeaf48`, 8건 반영)**: 인기 피드 비활성 정렬 드롭박스 제거, 룸 피드 정렬 커스텀 드롭다운+아이콘화(인기순=NavFlameIcon, 최신순=신규 ClockIcon), 게시글/댓글 자가투표 차단(`votes/actions.ts` castVote), 자가 북마크 차단+작성자 북마크 버튼 숨김(`bookmarks/actions.ts`), 글쓴이 1-Click 버튼/상태뱃지 텍스트를 `action_label_a/b` 대신 `vote_up_label`/`vote_down_label`과 1:1 매핑(`lib/flairAction.ts` 신설, 단일옵션 플레어인 룸 제안은 예외), 게시글 상세 뒤로가기 시 플레어 '전체' 고정+정렬 기억값 유지(`reset=1` 쿼리파라미터), 마이페이지 댓글 탭 삭제 버튼 추가+원글 링크를 게시글 상세로 수정, 짠수다 플레어 2건 라벨 문구 변경(DB).
- **결정 (2026-08-05, commit `59a2e57`, 1/3/4번 3건 반영)**: 룸(텍스트 유지)/플레어(타원형 `.flair-badge`) 표시 분리, 답글(대댓글) 정렬을 최신순→오래된순(작성 순서)으로 변경(대화 흐름상 @멘션 대상이 항상 위로 오도록), 마이페이지 프로필 상단의 '배지 보관함' 버튼 삭제(보상·명예 탭의 '내 배지 보관함'과 중복이라 정리).
- **결정 (2026-08-05, commit `40e6122`, 5-1/5-2/5-3번 3건 반영)**: 피드 카드에서 룸/플레어를 닉네임·게시시간과 같은 줄에 작게 배치(5-1), 피드 카드에도 `ImageCarousel` 재사용해 클릭 없이 스와이프로 첨부 이미지 전체 열람 가능 — 카드 전체가 Link라 넘김/점 인디케이터 클릭 시 상세로 이동해버리는 문제는 해당 버튼에 `stopPropagation` 추가로 해결(5-2), 클릭 전 미리보기를 2줄→6줄로 확대(5-3).
- **5-4(제목 필드 삭제) — 최종 미적용 결정**: 게시글 표시형태 개편 논의 중 나온 "제목 필드 삭제" 항목은 스키마·여러 화면에 걸친 구조적 변경이라 8/5 시점엔 별도 진행으로 분리해뒀음(commit `40e6122`). 이후 재검토를 거쳐 **최종적으로 미적용하기로 결정 — 게시글 제목 필드는 그대로 유지**함.
- **재검토 트리거**: 없음 (전 항목 반영 완료, 5-4는 명시적으로 종료된 결정 — 재적용 논의가 다시 필요해지면 새 항목으로 등록).
- **관련 코드**: `votes/actions.ts`, `bookmarks/actions.ts`, `lib/flairAction.ts`, `app/room/[code]/*`(RoomFeedControls), 마이페이지 댓글/게시글 탭 컴포넌트, `PostCard`, `ImageCarousel`

## 4. 서울 리전 이전(08-07) 후속 환경변수 재점검 — 처리 완료

- **현황**: 08-07 서울 리전 이전 직후엔 Vercel Kakao 로그인만 확인하고 넘어갔는데, 08-08에 VS Code+Claude Code로 개발 전환을 준비하며 전체 환경변수/스토리지 상태를 다시 훑어보니 이전 작업이 일부만 반영돼 있었음.
- **발견 1 — `SUPABASE_SERVICE_ROLE_KEY` 미이전**: Vercel 대시보드에서 `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`CRON_SECRET`은 08-07 이전 작업 때 같이 갱신됐지만(수정일 일치), `SUPABASE_SERVICE_ROLE_KEY`만 최초 생성일(7/25)에 머물러 있어 구(싱가포르) 프로젝트 키가 그대로 남아있었음. `lib/supabase/admin.ts`가 URL+이 키를 한 쌍으로 써서 관리자 클라이언트를 만드는데, 짝이 안 맞으면 인증 자체가 실패함 — 영향 범위: 블랙리스트 조회, 마이페이지 소프트 삭제, 게시글 작성자 액션, 알림, 월간 배지 크론, 시드 드립 크론. 새 프로젝트(`egutchfhvysqclxgyixj`)의 service_role 키로 교체 후 JWT payload를 디코딩해 `ref` 값이 새 프로젝트와 일치함을 확인, 재배포 완료.
- **발견 2 — 로컬 `.env.local` 전체 미이전**: `.env.local.example`(템플릿)이 구 프로젝트를 가리키는 건 이미 알고 있었는데, 실제 로컬 `.env.local`도 URL/anon key/service_role key 3개 전부 구 프로젝트 값 그대로였음. 즉 프로덕션(Vercel)은 새 프로젝트로 이전됐지만 로컬 개발 서버(`npm run dev`)는 계속 구 프로젝트에 붙어 있었던 상태. Supabase 대시보드 → 새 프로젝트 → API 설정에서 3개 값을 다시 복사해 `.env.local`에 반영 완료.
- **발견 3 — 실사용자 게시글 이미지 미이전**: `posts.image_urls` 전수 조회 결과 "책한권 사봤다"(사용자 `d98f7698-...`, 08-06 작성) 게시글의 이미지 2장이 구 프로젝트 Storage 도메인(`tmawuxjllizzixancfyg.supabase.co`)을 그대로 가리키고 있었음. 원본 파일은 구 프로젝트에 아직 살아있어 지금은 정상 노출되지만, 구 프로젝트를 pause/삭제하는 순간 깨질 예정이었음. 새 프로젝트 Storage로 파일 2개 이전 + DB의 `image_urls` 도메인 갱신 완료. 이 외 다른 게시글/`drafts`에는 구 프로젝트 참조 없음(전수 조회로 확인).
- **결정**: 위 3건 모두 즉시 수정하고 재배포 완료(2026-08-08). 코드/설정 파일 전체를 grep해 구 프로젝트(`tmawuxjllizzixancfyg`) 하드코딩이 남아있는지도 재확인 — `docs/schema.sql`·본 문서·`CLAUDE.md`의 역사 기록용 언급 3건 외에는 없음을 확인(이 3건은 의도된 기록이라 수정하지 않기로 함).
- **교훈**: 리전/프로젝트 이전 작업 시 "로그인 됐다"는 확인만으로는 부족함 — URL/anon key처럼 즉시 눈에 띄는 값 외에, service_role 키·로컬 `.env.local`·Storage에 저장된 기존 파일까지 별도로 전수 점검해야 함. 다음에 비슷한 이전 작업을 하면 이 4개 항목(Vercel 프로덕션 env, 로컬 `.env.local`, `.env.local.example`, Storage 파일)을 체크리스트로 삼을 것.
- **재검토 트리거**: 없음(처리 완료). 구 프로젝트(`tmawuxjllizzixancfyg`)를 pause/삭제하기 전에는 이 항목 전체를 다시 한번 확인할 것.
- **관련 코드**: `lib/supabase/admin.ts`, `lib/supabase/client.ts`, `.env.local`, `.env.local.example`, `posts.image_urls`

---

## 부록: 갱신 로그

| 날짜 | 변경 내용 |
|---|---|
| 2026-08-02 | v1 트래커 대체하여 신규 작성. 시드 데이터 재구성 대기, 룸/피드 성능 개선 항목 등록 |
| 2026-08-02 | 시드 콘텐츠 400개 인서트 완료, 페르소나 80명 확장 완료, 런칭일 클램프 버그 수정, `SEED_DRIP_LAUNCH_DATE` 잠정 2026-09-05 설정(로컬만), DB 레벨 드라이런 검증 결과 반영 |
| 2026-08-02 | 라이브 테스트 진행 — 실배포 엔드포인트 호출 중 풀 중복 발행 버그 발견 및 수정(`claim_seed_pool_items` 원자적 클레임), 테스트 데이터 정리, Vercel 환경변수 원복 필요 상태로 남음 |
| 2026-08-08 | 2번 항목에 서울 리전 이전 완료(2026-08-07) 후속 조치 반영 — 구조적 지연 요인 해소로 종결. 3번 항목(QA 중 수정요청사항 2026-08-03·08-05, 총 14건) 신규 등록 및 처리 완료 반영, 5-4(제목 필드 삭제) 최종 미적용 결정 명시 |
| 2026-08-08 | 1번 항목 남은 작업 처리 완료 반영(git push 확인/Vercel 환경변수 확정/회귀 테스트 완료), `SEED_DRIP_LAUNCH_DATE=2026-09-05` 최종 확정. 4번 항목(서울 이전 후속 환경변수 재점검 — service_role 키/로컬 .env.local/이미지 Storage 미이전 발견 및 수정) 신규 등록 |
